export class Stock {
    constructor ({ createdDate, productId, warehouseId, quantity }) {
        this.createdDate = createdDate
        this.productId = productId,
        this.warehouseId = warehouseId
        this.quantity = quantity
    }
}