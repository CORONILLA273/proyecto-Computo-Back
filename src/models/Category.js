export class Category {
    constructor ({ categoryName, categoryDescription, status }) {
        this.categoryName = categoryName
        this.categoryDescription = categoryDescription
        this.status = status
    }
}